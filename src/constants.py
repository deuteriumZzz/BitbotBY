"""
Неизменяемые программные константы.

Пользовательские настройки хранятся в config.py (загружается из .env).
Этот файл содержит только инварианты программы — числа, которые не
предназначены для настройки пользователем, но слишком важны чтобы
оставлять их голыми литералами по всему коду.

Правило: если одно и то же число встречается в ≥2 файлах, или если
магическое число требует комментария объясняющего почему оно существует —
оно принадлежит сюда.
"""

# ── Redis TTLs (seconds) ────────────────────────────────────────────────────

# 5 минут: данные OHLCV быстро устаревают; кэш режима использует то же
# окно, так как режим выводится из рыночных данных.
REDIS_TTL_MARKET_DATA: int = 300

# 24 часа: торговое состояние (позиции, портфель) и суточные счётчики
# (AI-бюджет, статистика производительности) сбрасываются ежедневно.
REDIS_TTL_TRADING_STATE: int = 86_400

# 7 дней: веса обученной модели дорого вычислять; храним достаточно
# долго чтобы пережить выходные без принудительного переобучения.
REDIS_TTL_MODEL: int = 604_800

# ── Position monitoring ─────────────────────────────────────────────────────

# Удалять символ из монитора после этого числа последовательных ошибок.
# Пять попыток дают достаточный допуск на кратковременные сбои сети,
# не позволяя постоянно падающему символу блокировать весь цикл монитора.
MONITOR_MAX_ERRORS: int = 5

# ── Trade sizing ────────────────────────────────────────────────────────────

# Количество последних живых сделок для расчёта win rate и EV.
# 50 достаточно для статистической значимости, но мало enough чтобы
# отражать текущий рыночный режим, а не устаревшую историю.
TRADE_HISTORY_LOOKBACK: int = 50

# Минимум живых сделок до доверия Kelly-сайзингу.
# Ниже этого порога оценка win rate имеет слишком широкий CI;
# используем консервативный фиксированный фракционный кэп вместо Kelly.
KELLY_MIN_TRADES: int = 10

# ── Alerts ──────────────────────────────────────────────────────────────────

# Минимальный интервал (сек) между повторными Telegram-алертами о тишине.
# Один час предотвращает шторм алертов в спокойный период (например,
# низковолатильные выходные) без маскировки реальных проблем.
SILENT_DEATH_ALERT_COOLDOWN: int = 3_600
