server {
    listen 8080;

    location / {
        auth_basic "BitbotBY Dashboard";
        auth_basic_user_file /etc/nginx/htpasswd;

        proxy_pass http://dashboard:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
